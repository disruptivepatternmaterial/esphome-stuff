---
wifi:
  networks:
    - ssid: !secret wifi_ssid
      password: !secret wifi_password
    - ssid: !secret wifi_ssid_2
      password: !secret wifi_password_2
  reboot_timeout: 1min
  #ap:
  #  ssid: ${friendlyname}
  #  password: !secret ap_password
  domain: .iot.baargsiitsch.com
